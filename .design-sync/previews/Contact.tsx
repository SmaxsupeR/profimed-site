import { Contact } from 'profimed-site';

// Один экспорт намеренно: presetDirection меняет только выбранное значение
// в выпадающем списке внутри формы, и вторая карточка выглядела копией первой.
// Как работает предвыбор — показано в превью BookingForm.

export const Default = () => <Contact />;
