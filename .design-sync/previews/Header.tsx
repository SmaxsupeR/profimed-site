import { Header } from 'profimed-site';

// Шапка липкая и во всю ширину — один экспорт, смотреть на неё в двух
// вариантах смысла нет. Мобильное состояние (бургер) статически не снять:
// меню открывается по клику.

export const Default = () => <Header />;
