// Юридические реквизиты клиники — одна точка правды для подвала, страницы
// политики конфиденциальности и записи о согласии, уходящей в CRM.
//
// Значения вида [УКАЗАТЬ: …] — незаполненные поля. Пока такое поле на месте,
// подвал и страница политики молча скрывают соответствующую строку, а в
// консоли разработки печатается предупреждение (см. низ файла). Выдуманный
// номер лицензии у медицинской клиники хуже, чем его отсутствие, поэтому
// заглушка намеренно выглядит как явный пропуск, а не как правдоподобная
// строка, которую легко принять за настоящую.
//
// Фактический адрес приёма, телефоны и почта клиники здесь НЕ дублируются:
// они живут в i18n/dict.js (ключи con/top), координаты — в data/clinic.js.
// Здесь только юридическая сторона. Юридический адрес хранится отдельно от
// фактического специально: у клиники они различаются (5-Кургонча против
// Мирабада), и подставлять один вместо другого нельзя.

// ВНИМАНИЕ: этот файл импортируется подвалом, то есть попадает в бандл
// главной страницы и виден в исходниках любому посетителю — даже те поля,
// которые нигде не выводятся на экран. Поэтому сюда кладём только то, что
// и так предназначено для публикации. Банковские реквизиты (расчётный
// счёт, МФО, банк), ОКЭД и ФИО директора из договоров сознательно НЕ
// перенесены: на сайте они не нужны, а в бандле светились бы без пользы.
// Если они понадобятся (счёт, страница реквизитов) — их место на сервере,
// а не в клиентском коде.

const TODO = '[УКАЗАТЬ: ';

export const LEGAL = {
  // Оператор персональных данных. Это ООО, которому принадлежит клиника
  // ProfiMed: оно же указано исполнителем медицинских услуг в договоре и
  // оно же держатель лицензии. Название клиники и название юрлица не
  // совпадают — в реквизитах должно стоять юрлицо, а не вывеска.
  // Написание через дефис — как в лицензии («HOJIAKBAR -BESH») и в блоке
  // реквизитов обоих договоров («HOJIAKBAR-BESH» / OOO «HOJIAKBAR-BESH»).
  // В преамбулах договоров встречается и вариант без дефиса — расхождение
  // в самих документах; берём то написание, которое стоит в реквизитах и
  // в лицензии, а не то, что в тексте преамбулы.
  entityName: 'Общество с ограниченной ответственностью «HOJIAKBAR-BESH»',
  entityNameShort: 'ООО «HOJIAKBAR-BESH»',

  taxId: '200843529',

  // Лицензия на медицинскую деятельность (данные сверены по самому
  // документу). В лицензии наименование записано как «HOJIAKBAR -BESH» —
  // через дефис и с пробелом перед ним; это расхождение с уставным
  // написанием через пробел оставлено как есть, не «исправлено» нами.
  licenseNumber: '1293625',
  // Не одна дата выдачи, а срок действия: в документе указан период
  // «03.12.2020dan - 05.01.2031gacha». Показывать только дату начала было
  // бы неполно — пациенту важнее, что лицензия действует сейчас.
  licenseValidFrom: '03.12.2020',
  licenseValidTo: '05.01.2031',
  licenseIssuer: 'Министерство здравоохранения Республики Узбекистан',
  // Порядковый номер по реестру лицензий — по нему запись проверяется в
  // государственном реестре. На странице не показывается: для пациента
  // достаточно номера лицензии, а лишние цифры только утяжеляют блок.
  // Лежит здесь, чтобы не искать документ, когда номер понадобится.
  licenseRegistryNumber: '24748672',

  // Взято из блока «Юридические адреса сторон» русского договора. В
  // лицензии тот же адрес записан по-узбекски и без номера дома
  // («Шайхонтохур тумани, QO‘RG‘ONCHA-5 KO‘CHASI») — номер дома есть
  // только в договоре, поэтому источником выбран он.
  legalAddress: 'г. Ташкент, ул. 5 Курганча, 5',

  // Канал для обращений по вопросам обработки персональных данных: отзыв
  // согласия, запрос на удаление или исправление.
  //
  // Здесь намеренно НЕТ фамилии сотрудника. Оператор персональных данных —
  // юрлицо, а не человек; кто внутри компании обслуживает сайт, посетителю
  // знать незачем, и публиковать чьё-то ФИО в политике только потому, что
  // он технически отвечает за сайт, — лишнее раскрытие персональных данных
  // в документе про защиту персональных данных.
  //
  // ЕДИНСТВЕННОЕ, ЧТО ОСТАЛОСЬ ПОДТВЕРДИТЬ: что info@profimed.uz
  // утверждается как официальный адрес именно для таких обращений.
  privacyEmail: 'info@profimed.uz',

  // Жалобы и предложения по работе клиники — ДРУГАЯ роль и другой канал.
  // В политике обработки персональных данных эти контакты не показываются:
  // человек, пришедший отзывать согласие, не должен попасть к сотруднику,
  // который занимается качеством обслуживания, и наоборот. Выводятся в
  // секции контактов на главной.
  // По языкам, как и имя врача в i18n/dict.js (doc.d1Name) — та же логика:
  // ФИО реального человека не должно переключаться со своим алфавитом,
  // подставляя кириллицу на латинской узбекской странице. ru/uzc — то же
  // ФИО в написании «Фамилия Имя», как в документах; uz/en — «Имя Фамилия»
  // в транслитерации, тем же приёмом, что и doctorName в dict.js
  // («Усманов Акбар Агзамович» → «Akbar Usmanov» для en). Латинское и
  // английское написание имени — предположительное (транслитерация по той
  // же схеме), и требует подтверждения клиникой, как и сказано в брифе.
  complaintsOfficer: {
    ru: 'Рустамбекова Наргиза',
    uzc: 'Рустамбекова Наргиза',
    uz: 'Nargiza Rustambekova',
    en: 'Nargiza Rustambekova',
  },
  complaintsPhone: '+998 99 895 35 00',

  // Дата редакции политики. Показывается пациенту вверху страницы и
  // уходит в CRM вместе с заявкой как privacyPolicyVersion — по ней потом
  // видно, с какой именно редакцией человек согласился. Обновлять при
  // каждом изменении текста политики.
  policyUpdatedAt: '2026-08-25',

  // Человекочитаемая дата редакции для заголовка страницы, по локалям.
  // Держим строками, а не форматируем Intl.DateTimeFormat: у узбекского
  // в обоих алфавитах форматирование дат в браузерах разъезжается, а
  // документ должен читаться одинаково везде.
  policyUpdatedLabel: {
    ru: '25 августа 2026 года',
    uz: '2026-yil 25-avgust',
    uzc: '2026-йил 25-август',
    en: '25 August 2026',
  },
};

export const LEGAL_GAPS = Object.entries(LEGAL)
  .filter(([, value]) => typeof value === 'string' && value.startsWith(TODO))
  .map(([key]) => key);

// Незаполненное поле не должно попасть на страницу. Проверка вынесена в
// функцию, потому что нужна в трёх местах (подвал, реквизиты на странице
// политики, контакты для обращений) и везде одинаковая.
export const isFilled = (value) =>
  typeof value === 'string' && value.length > 0 && !value.startsWith(TODO);

// import.meta.env.DEV — только сборка для разработки; в прод-бандл эта
// ветка не попадает (Vite вырезает её при минификации). Проверка на
// существование самого import.meta.env нужна, чтобы файл можно было
// импортировать и обычным node — вне Vite этого объекта нет, и без
// проверки любой скрипт, читающий реквизиты, падал бы на ровном месте.
if (typeof import.meta.env !== 'undefined' && import.meta.env.DEV && LEGAL_GAPS.length > 0) {
  console.warn(
    `[legal.js] Не заполнены реквизиты: ${LEGAL_GAPS.join(', ')}.\n` +
    'Пока они пустые, подвал и страница политики скрывают соответствующие ' +
    'строки, чтобы не публиковать заглушки. Заполните src/data/legal.js.'
  );
}
