import { YANDEX_REVIEWS_URL, TWOGIS_REVIEWS_URL } from './clinic.js';

// Этап 3.19 (коррект.) — раньше этот массив сам БЫЛ источником данных для
// Reviews.jsx. Теперь это только аварийный посевной набор для
// hooks/useReviewRatings.js: тот пытается получить реальные данные с
// публичного API (GET /api/public/reviews-ratings, которого пока не
// существует — см. комментарий в самом хуке), и падает сюда, только если
// ни живого ответа, ни кэша прошлой удачной загрузки ещё не было (самый
// первый заход на сайт, backend ещё не подключён). Reviews.jsx этот файл
// напрямую не импортирует и не должен — вся логика «откуда взять данные»
// живёт в хуке, секция знает только про его результат.
//
// Будущий путь данных целиком: закрытый Telegram-бот клиники → защищённый
// backend endpoint (администратор/бот пишет туда) → база данных →
// публичный GET-эндпоинт (сайт читает оттуда, без секретов на фронтенде) →
// этот хук → Reviews.jsx. Ни секретов, ни самого бота, ни backend-роута в
// этом фронтенд-проекте нет и не должно быть — только граница, через
// которую они потом подключатся.
//
// rating/reviewCount — намеренно null, а не 0 и не «примерно»: без
// подтверждённой проверки это ровно то приблизительное значение, которое
// нельзя выдавать за актуальное (в старых комментариях этого проекта
// проскакивали цифры «650 и 88 оценок» без даты проверки — ровно тот
// случай). checkedAt/publishedAt — по той же причине null: датой не
// сопровождать нечего, пока нет самого значения.
export const REVIEWS_RATINGS_FALLBACK = [
  {
    source: 'yandex',
    rating: null,
    reviewCount: null,
    profileUrl: YANDEX_REVIEWS_URL,
    checkedAt: null,
    publishedAt: null,
    visible: true,
  },
  {
    source: '2gis',
    rating: null,
    reviewCount: null,
    profileUrl: TWOGIS_REVIEWS_URL,
    checkedAt: null,
    publishedAt: null,
    visible: true,
  },
];
