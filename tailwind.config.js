/** @type {import('tailwindcss').Config} */
// primary/slate ниже — тёплая тил-палитра редизайна 2026 года (см. комментарии
// у самих токенов). Старый синий/оливковый, восстановленный со снимка сайта
// 2021 года, остался только в `leaf` — он ещё используется в не тронутых
// редизайном компонентах и будет выведен файл за файлом на следующих этапах.
export default {
  // .design-sync/previews — карточки компонентов для дизайн-системы. Без них
  // в этом списке классы, которые встречаются только в превью (сетки, ширины),
  // не попадают в собранный CSS, и карточки уезжают полуоформленными.
  content: ['./index.html', './src/**/*.{js,jsx}', './.design-sync/previews/**/*.{ts,tsx}'],
  // Класс, а не media-стратегия — тема переключается вручную (ThemeContext
  // ставит/снимает .dark на <html> сама, в том числе когда выбор пользователя
  // расходится с системной темой).
  darkMode: 'class',
  theme: {
    extend: {
      fontFamily: {
        // Literata — тёплая книжная антиква для заголовков (не стерильно-
        // больнично), Inter — для основного текста и цифр в прайсе.
        //
        // Важно: у обоих есть настоящая кириллица. Первым кандидатом был
        // Fraunces, и он молча не работал — у него вообще нет кириллических
        // начертаний, поэтому все русские заголовки падали в запасной Georgia.
        // Любую замену здесь проверять так же: ls files/ | grep cyrillic.
        display: ['Literata Variable', 'Georgia', 'serif'],
        sans: ['Inter Variable', 'system-ui', 'Segoe UI', 'Roboto', 'sans-serif'],
      },
      colors: {
        // Тёплая тил-палитра редизайна (2026) — заменяет синий #1187C5,
        // восстановленный из снимка старого сайта. primary-600 = #2E5653 —
        // точный оттенок, заданный пользователем напрямую (не подобран на
        // глаз), остальные ступени дотянуты вокруг него тем же оттенком
        // (H≈175°), светлее/темнее по насыщенности и светлоте.
        primary: {
          50: '#F0F5F4',
          100: '#E0EBEA',
          200: '#C0D8D6',
          300: '#8FBDB9',
          400: '#53938E',
          500: '#3F736F',
          600: '#2E5653',
          700: '#244744',
          800: '#193331',
          900: '#102321',
        },
        // Тёплый нейтральный / чернильный ряд поверх встроенной палитры
        // Tailwind `slate` — это осознанный оверрайд по имени, а не новый
        // токен: почти все компоненты уже стилизованы классами slate-*, и
        // переопределение самого ключа перекрашивает их все разом, без
        // ручной замены классов в каждом файле.
        slate: {
          50: '#F7F8F5',
          100: '#EEF1EE',
          200: '#DDE4E0',
          300: '#C7D0CC',
          400: '#96A39E',
          500: '#71807A',
          600: '#5C6B65',
          700: '#43534D',
          800: '#29413B',
          900: '#17332F',
          950: '#102722',
        },
        leaf: {
          50: '#F5F7E7',
          100: '#E9EEC7',
          200: '#D3DD97',
          300: '#BACB68',
          400: '#A8BC4C',
          500: '#9AAC34',
          600: '#7C8A2A',
          700: '#616C22',
          800: '#4B531B',
          900: '#3B4116',
        },
        // Второй и третий акцент, снятые с onemedical.com/virtual-care один
        // в один (secondary = их «lime», melon = их «melon») — по прямому
        // запросу пользователя, поверх primary (#2E5653), который остаётся
        // основным. Токены объявлены, но пока нигде не применены: куда
        // именно ставить лайм/дыню в компонентах — отдельное дизайн-решение,
        // не часть этой правки.
        secondary: {
          50: '#F6FAEE',
          100: '#EBF4DA',
          200: '#D7E9B5',
          300: '#C0DC91',
          400: '#ABCC76',
          500: '#92B85C',
          600: '#799E47',
          700: '#618036',
          800: '#4A6329',
          900: '#35491E',
        },
        melon: {
          50: '#FFF8EE',
          100: '#FFEED6',
          200: '#FFDCAD',
          300: '#FFCE8F',
          400: '#FFC776',
          500: '#F5AA4A',
          600: '#DB8A2E',
          700: '#B56E22',
          800: '#8C5419',
          900: '#663C11',
        },
      },
      boxShadow: {
        card: '0 1px 2px 0 rgb(16 40 55 / 0.05), 0 1px 3px 0 rgb(16 40 55 / 0.08)',
        'card-hover': '0 8px 24px -6px rgb(16 40 55 / 0.14)',
      },
    },
  },
  plugins: [],
};
